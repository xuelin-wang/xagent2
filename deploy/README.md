# Deployment Assets

This folder holds shared deployment assets (Helm charts, Kustomize bases,
Terraform modules, etc.) that are reused across multiple projects.

Project-specific overrides and environment configs live under:
`projects/<project>/deploy/`.
